meta {
  name: Generate Names - Fantasy Generated
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/generate_name.php?count=5&gender=female&culture=elven&method=fantasy_generated&type=full_name&period=ancient
  body: none
  auth: none
}

headers {
  X-API-KEY: {{apiKey}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response contains names array", function() {
    const data = res.getBody();
    expect(data).to.have.property('names');
    expect(data.names).to.be.an('array');
  });
  
  test("Returns correct count of names", function() {
    const data = res.getBody();
    expect(data.names.length).to.equal(5);
  });
}
